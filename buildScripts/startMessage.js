//var chalk =require('chalk'); // es5 module syntax
import chalk from 'chalk';  // es6 module syntax, uses babel to transpilie

console.log(chalk.green('Starting app in dev mode...')); // eslint-disable-line no-console
